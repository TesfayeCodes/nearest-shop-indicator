# Backend

This folder contains the backend API and business logic for the Nearest Shop Indicator.

## Folder Structure

- `app/` — Main application code
  - `routes/` — API route definitions
  - `models/` — Database models/schemas
  - `services/` — Core logic (e.g., distance calculations)
  - `utils/` — Utility functions
- `tests/` — Unit and integration tests
- `requirements.txt` — Python dependencies
- `Dockerfile` — Backend containerization

## Running the Backend

See the root README or this file for setup instructions as your backend grows.
