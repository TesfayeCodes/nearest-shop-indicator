# Nearest Shop Indicator

This project helps users find the nearest shop. It consists of a frontend, a backend, and comprehensive infrastructure for deployment and development.

## Structure

- `backend` — REST API and core logic
- `frontend` — User interface for interacting with shop data
- `infrastructure` — All deployment and orchestration resources, database scripts, CI/CD, and environment configs

See README files in each subdirectory for specific instructions.
